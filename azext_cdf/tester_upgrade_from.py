# get expect to fail from params, from expect obj, or defaults to false
# git fetch --tags --all
# git --no-pager tag --sort=-creatordate
# git fetch --tags --all
# git --no-pager show HEAD~3 --pretty=format:"%H" --quiet
