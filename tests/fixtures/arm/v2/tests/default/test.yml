description: "Default"
params:
  tags: "{{ vars.tags }}"

vars:
  tags:
    test: "default"
upgrade_strategy: all
expect:
  up:
    fail: false
    jassert:
      - "{{ result.outputs.name.value == 'helloworld' }}"
    plan:
      create: 1
      # delete: 2
